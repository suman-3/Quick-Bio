import React from 'react'


const getBio = async () => {
  try {
    const res = await fetch('')
  } catch (error) {
    
  }
}

const BioCard = () => {
  return (
    <div>
      Bio Card
    </div>
  )
}

export default BioCard
